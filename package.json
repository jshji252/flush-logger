{
  "name": "flush-logger",
  "version": "1.0.0",
  "description": "simple logger that writes to disk, when either a time limit is reached or a log count threshold is exceeded (i.e., 5 seconds or 20 logs)",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "logger",
    "rxjs"
  ],
  "author": "underwater",
  "license": "ISC",
  "dependencies": {
    "rxjs": "^7.8.1"
  }
}
